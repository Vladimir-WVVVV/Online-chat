# MySQL init directory

Spring Boot runs `schema.sql` in `backend/src/main/resources` during local startup. This directory is reserved for Docker entrypoint SQL files used in later deployment stages.

